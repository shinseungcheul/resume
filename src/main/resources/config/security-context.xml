<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:sec="http://www.springframework.org/schema/security"
       xmlns="http://www.springframework.org/schema/beans"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:c="http://www.springframework.org/schema/c"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
	   http://www.springframework.org/schema/beans/spring-beans.xsd
	   http://www.springframework.org/schema/security
	   http://www.springframework.org/schema/security/spring-security.xsd">

    <sec:global-method-security secured-annotations="enabled" pre-post-annotations="enabled" />

    <!--<bean id="bcrypt" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" c:strength="11"/>-->

    <sec:http pattern="/resources/**" security="none"/>

    <sec:http auto-config="true" use-expressions="true">
        <sec:form-login login-page="/login"
                        login-processing-url="/login/process"
                        authentication-success-forward-url="/resume/index"
                        authentication-failure-forward-url="/error"
                        username-parameter="user_id"
                        password-parameter="password"
                        default-target-url="/"
                        always-use-default-target="true"
        />

        <sec:logout delete-cookies="RESUME" logout-url="/logout"/>
        <sec:intercept-url pattern="/login" access="permitAll()"/>
        <sec:intercept-url pattern="/write/**" access="hasRole('ROLE_ADMIN')"/>
        <sec:intercept-url pattern="/**" access="permitAll()"/>

        <sec:session-management invalid-session-url="/resume/index">
        </sec:session-management>
        <sec:session-management>
            <sec:concurrency-control max-sessions="1" error-if-maximum-exceeded="true"/>
        </sec:session-management>


    </sec:http>

    <sec:authentication-manager >
        <sec:authentication-provider>
            <sec:user-service>

            </sec:user-service>
        </sec:authentication-provider>
    </sec:authentication-manager>


</beans>